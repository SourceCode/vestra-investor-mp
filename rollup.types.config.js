export default {
    // ... other config options
    output: {
        dir: 'types-dist', // This must be exactly 'types-dist'
        format: 'es',
        preserveModules: true,
        preserveModulesRoot: 'src',
    },
    // ... plugins and other options
};